{
  "name": "toender",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:corpus": "node scripts/build_strudel_corpus.mjs && node scripts/build_sccode_corpus.mjs",
    "prebuild": "npm run build:corpus",
    "predev": "npm run build:corpus",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "idb": "^8.0.3",
    "wavesurfer.js": "^7.10.0"
  },
  "devDependencies": {
    "vite": "^8.0.10",
    "vite-plugin-pwa": "^1.3.0",
    "workbox-window": "^7.4.1"
  }
}
